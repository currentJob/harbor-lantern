# 성능 측정 리포트 (AC-043 · NFR-010)

이 표는 **아래 환경에서 방금 측정한 값**이다. 설계서 §8 의 목표치는 보증이 아니라 목표이고,
절대치는 실행 환경에 의존한다(가정 A6).

- 측정 시각: `2026-09-13T15:33:47Z`
- Python: `3.12.13` · 플랫폼: `Windows 11 (AMD64)`
- 표본: 종목당 200회 (워밍업 20회 제외)
- 시드 스팟 수: 27
- 측정 대상: 서버 핸들러 내부 처리시간 (X-Process-Time, ms). 네트워크·브라우저 제외

| 종목 | p50 | p95 | p99 | 최소 | 최대 | 목표 p50 | 목표 p95 | 목표 충족 | 파탄 상한(1000ms) |
|------|-----|-----|-----|------|------|---------|---------|----------|------------------|
| `GET /api/trips/{trip_id}/state` | 4.46 ms | 5.29 ms | 6.60 ms | 4.13 ms | 17.27 ms | ≤ 50 ms | ≤ 120 ms | 예 | 이내 |
| `POST /api/trips/{trip_id}/days/2/optimize` | 2.55 ms | 2.88 ms | 3.97 ms | 2.36 ms | 4.29 ms | ≤ 150 ms | ≤ 150 ms | 예 | 이내 |

CI 는 목표치로 실패하지 않는다 — 러너 성능이 흔들리면 게이트가 간헐적으로 빨개지고,
간헐적 실패는 아무도 안 보게 된다. 실패는 파탄 상한(p95 > 1000ms)에서만이다.
