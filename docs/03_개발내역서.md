# 개발내역서 — 홍콩 3박4일 동적 여행 가이드 (Harbor Lantern)

| 항목 | 내용 |
|------|------|
| 문서번호 | DOC-HKG-03 |
| 프로젝트 | 홍콩 3박4일 동적 여행 가이드 (harbor-lantern) |
| 버전 | v1.1 |
| 작성일 | 2026-09-14 (v1.1 개정 2026-09-14) |
| 상태 | 확정 |
| 작성자 | product-build-orchestrator 파이프라인 (code-implementation 단계) |
| 선행문서 | `01_요구사항정의서.md` (v1.1) · `02_설계서.md` (v1.2) · `contracts/openapi.yaml` |

## 개정이력

| 버전 | 일자 | 변경 내용 |
|------|------|----------|
| v1.0 | 2026-09-14 | 최초 작성. T0~T3 구현 완료분 기록(소스 79파일·테스트 561건) |
| v1.1 | 2026-09-14 | REQ-017·REQ-018 구현 반영: DSN-26(Overpass 어댑터·위치 키 캐시·도메인 정규화)·DSN-27(근처 장소 화면) 매핑 추가. 실제 Overpass 조회로 침사추이 반경 500m 49건 확인 |

---

## 1. 구현 개요

참조 정적 페이지(`reference/original-static-page.html`)를 **서버를 가진 동적 애플리케이션**으로 옮겼다.
원본에서 달라진 본질은 세 가지다.

| 원본 | 구현 결과 |
|------|----------|
| `DAYS` 배열이 JS에 하드코딩 | `seed/spots.json` → SQLite 주입, API로 **편집 가능**(REQ-004·REQ-005) |
| 완료 체크가 `localStorage` (기기 1대 고립) | 서버 저장 + **초대코드로 동행과 공유**(REQ-002·REQ-003·REQ-006) |
| 클라이언트 계산만 (거리·정렬) | 이동시간·도착예상·경고·충돌·정산·동선최적화를 **서버가 계산**(REQ-008~REQ-013) |

`localStorage`는 폐기되지 않고 **역할이 축소**됐다 — 이제 참가자 토큰만 보관한다(기기 로컬 값).

**원커맨드**

| 목적 | 명령 | 확인 |
|------|------|------|
| 실행 | `uv run python -m harbor_lantern` | `GET /api/health` → 200, `GET /` → 200 (5,814B) |
| 테스트 | `uv run pytest -q` | **605 passed** |
| 린트 | `uv run ruff check .` | All checks passed |
| 성능 | `uv run python tools/perf_report.py` | `reports/perf.json`·`perf.md` |

---

## 2. 작업 분할과 실제 진행

설계서 §10의 DAG(T0 선행 → T1·T2·T3 병렬)를 그대로 실행했다. 파일 소유권이 겹치지 않게 나눈 것이
병렬 실행의 전제였고, **실제로 세 담당 사이의 편집 충돌은 0건**이었다.

| 작업 | 범위 | 결과 |
|------|------|------|
| T0 | 공유 골격(`pyproject.toml`·`uv.lock`·`config`·`clock`·`models`·`util`·`schema.sql`·`seed/spots.json`·테스트 픽스처) | 완료 |
| T1 | 도메인 계산 엔진 10모듈 + `tests/domain/` | 완료 (도메인 테스트 136건) |
| T2 | 저장소·서비스·API + `tests/api/` + 성능 하네스 | 완료 (API 테스트 163건) |
| T3 | 프론트(HTML·CSS·JS 14모듈)·Leaflet 벤더링 + `tests/static/` + README | 완료 (정적 테스트 40건) |

**중단과 재개(기록해 둘 사실).** T1·T2·T3 병렬 실행 중 **API 세션 한도(HTTP 429)로 세 작업이 동시에
중단**됐다. 이때 디스크에 남은 산출물을 측정한 결과 도메인·프론트는 사실상 완성, API는 배선까지 완료돼
있었고 **진입점(`__main__.py`)·API 테스트·정적 테스트·README가 비어 있었다.** 재개 시 "이미 끝난 것"과
"남은 것"을 명시해 넘겼기 때문에 **재작업 없이 잔여분만 채웠다.** 중단 지점을 측정하지 않고 재개했다면
같은 파일을 두 번 만들었을 자리다.

---

## 3. 매핑

### 3.1 DSN → 구현 파일

| DSN | 설계 항목 | 구현 |
|-----|----------|------|
| DSN-01 | 단일 프로세스·계층 분리 | `api/app.py` (FastAPI + StaticFiles 마운트) |
| DSN-02 | 포트/어댑터 경계 | `services/external/ports.py` · 계층 검사 `tests/static/test_layering.py` |
| DSN-03 | 시계 포트 | `clock.py` (`Clock`·`SystemClock`·`FixedClock`) |
| DSN-04 | 설정 단일 소스 | `config.py` (`TravelConfig`·`ExternalConfig`·`Settings`·`TIME_BANDS`) |
| DSN-05 | SQLite 스키마·WAL·낙관적 잠금 | `storage/schema.sql` · `storage/db.py` |
| DSN-06 | 시드 데이터 + 주입 | `seed/spots.json` · `storage/seed.py` · `contracts/seed-spots.schema.json` |
| DSN-07 | 초대코드 생성·정규화 | `domain/invite.py` |
| DSN-08 | 참여·토큰·레이트 리밋 | `services/trip_service.py` · `api/ratelimit.py` |
| DSN-09 | 스팟 CRUD·낙관적 잠금 | `services/spot_service.py` · `storage/repo_spots.py` · `api/routes/spots.py` |
| DSN-10 | 재정렬·일자 간 이동(원자) | `storage/repo_spots.py` (2단계 UPDATE, `BEGIN IMMEDIATE`) |
| DSN-11 | 완료 체크·진행률 | `services/spot_service.py` · `services/plan_service.py` |
| DSN-12 | 경비·분담 배분 | `domain/money.py` · `services/expense_service.py` · `storage/repo_expenses.py` |
| DSN-13 | 최소 송금 정산 | `domain/settle.py` |
| DSN-14 | 거리·이동시간 엔진 | `domain/geo.py` · `domain/travel.py` |
| DSN-15 | 타임라인·도착 예상시각 | `domain/timeline.py` |
| DSN-16 | 영업시간 파서 | `domain/hours.py` |
| DSN-17 | 닫힘·휴무 경고 | `domain/warn.py` |
| DSN-18 | 고정시각 충돌 검사 | `domain/conflict.py` |
| DSN-19 | 동선 최적화(앵커 보존) | `domain/route.py` |
| DSN-20 | 외부 공급자 캐시·폴백 | `services/external/{cache,openmeteo,frankfurter}.py` · `storage/repo_cache.py` |
| DSN-21 | 리비전 ETag/304 폴링 | `services/plan_service.py` · `api/routes/trips.py` |
| DSN-22 | 프론트·UX 이관·타일 폴백 | `web/index.html` · `web/css/app.css` · `web/js/**`(14모듈) · `web/vendor/leaflet-1.9.4/` |
| DSN-23 | 성능 측정 하네스 | `tools/perf_report.py` · `X-Process-Time` 미들웨어 |
| DSN-24 | 패키징·원커맨드·버전 고정 | `pyproject.toml` · `uv.lock` · `__main__.py` |
| DSN-25 | LICENSE·CI·릴리스 | `LICENSE`(MIT) · `.github/workflows/{ci,release}.yml` · `.github/quality/` |
| DSN-26 | Overpass 어댑터 + 위치 키 캐시 | `services/external/overpass.py` · `services/external/nearby.py` · `domain/places.py` · `config.NEARBY_CATEGORIES` |
| DSN-27 | 근처 장소 화면 | `web/js/render/nearby.js` · `web/js/map.js`(별도 레이어) · `web/js/main.js` · `api/routes/external.py` |

### 3.2 REQ → 구현 진입점

| REQ | 구현 진입점 |
|-----|------------|
| REQ-001 | `POST /api/trips` → `storage/seed.py` (4일 27스팟 주입) |
| REQ-002 | `domain/invite.py` → `trip.invite_code` (12자, 하이픈 표시형 분리) |
| REQ-003 | `POST /api/join` |
| REQ-004 | `POST/PATCH/DELETE /api/trips/{id}/spots...` |
| REQ-005 | `PUT .../days/{n}/order` · `POST .../spots/{id}/move` |
| REQ-006 | `PUT .../spots/{id}/done` (누가·언제 기록) |
| REQ-007 | `POST/PATCH/DELETE .../expenses` |
| REQ-008 | `GET .../settlement` → `domain/settle.py` |
| REQ-009 | `POST .../days/{n}/optimize` → `domain/route.py` |
| REQ-010 | `state.days[].totals` · `spot.leg_to_next` |
| REQ-011 | `spot.schedule.eta_local` / `depart_local` |
| REQ-012 | `state.warnings[kind=closed_on_arrival 등]` |
| REQ-013 | `state.conflicts[]` |
| REQ-014 | `GET /api/weather` |
| REQ-015 | `GET /api/fx` · 경비 `amount_krw` |
| REQ-016 | `GET .../state` + `ETag` / `If-None-Match` → 304 |
| REQ-017 | `GET /api/nearby?lat=&lng=&radius_m=&category=` |
| REQ-018 | 조회 결과 → 기존 `POST .../days/{n}/spots` (별도 경로 없음) |

---

## 4. 설계 대비 변경 (사유 병기)

| # | 변경 | 사유 |
|---|------|------|
| 1 | `/healthz` → **`/api/health`** | 구현·프론트·정적 내보내기가 `/api/` 접두어를 전제한다. `/api/*` 안에 두면 CORS·`Cache-Control: no-store` 규칙을 예외 없이 받는다. 설계서 v1.1에서 정정 완료 |
| 2 | `GET /` 를 StaticFiles 마운트가 아닌 **명시 라우트**로 | 마운트는 OpenAPI에 나오지 않아 계약의 `/` 가 대조되지 않았다. 동작은 동일 |
| 3 | OpenAPI 8개 오퍼레이션에 **`422` 추가** | FastAPI는 경로 파라미터·본문이 있는 모든 오퍼레이션에 422를 자동 문서화한다. 계약에 예외를 두면 그 예외가 드리프트의 문이 된다 — 계약이 사실을 적게 했다 |
| 4 | **`X-Process-Time` 미들웨어 신설** | 없으면 성능 하네스가 "서버 핸들러 내부 처리시간"을 잴 근거가 없다. 헤더가 없으면 하네스가 즉시 실패한다(벽시계로 조용히 대체하지 않는다) |
| 5 | `JoinRequest`에 Pydantic 길이 제약을 **걸지 않음** | Pydantic 검증은 핸들러 이전에 돌아 "레이트 리밋이 가장 먼저"라는 설계 §6.17 순서를 깬다. 길이 검사는 서비스가 하고 같은 422를 낸다 |
| 6 | `SpotInput`·`RouteProposal` 타입을 `models.py`에 **신설** | 설계가 이름만 쓰고 필드를 주지 않았다. 두 담당이 각자 만들면 경계가 갈라진다 |
| 7 | 도메인 순수성 검사에서 **`config` 임포트는 허용** | 설계 §6.7 시그니처(`leg(a, b, cfg)`)가 그 타입을 요구한다. 검사가 설계를 넘어선 것이라 되돌렸다 |
| 8 | **`.gitattributes` 신설** (`web/vendor/** -text`) | 이 PC는 `core.autocrlf=true`다. 벤더 파일이 체크아웃에서 CRLF로 바뀌면 MANIFEST 해시가 전부 깨진다(1바이트 변조로 재현 확인) |
| 9 | **`tools/export_pages.py` · `web/config.js`** (설계 범위 밖) | 화면만 정적 호스팅하고 API는 별도 출처로 두는 내보내기 경로. 기본값(`apiBase: ''` = 동일 출처)에서 **동작이 완전히 동일**하고 외부 도메인 참조도 늘지 않는다. 요구사항에 없던 추가분이므로 **MVP 검증 대상이 아니다** |
| 10 | `reports/perf.json`·`perf.md`를 **gitignore 예외로 추적** | AC-043의 증거물이다. 저장소에 없으면 "측정했다"는 문장만 남고 측정값이 사라진다 |
| 11 | Leaflet을 GitHub 릴리스 zip 대신 **npm 타르볼**에서 | zip 배너에 빌드 해시가 붙어 11바이트가 다르다. npm 쪽은 `dist.integrity`/`shasum`과 대조 가능해 무결성 근거가 명확하다(둘 다 일치 확인) |

---

## 5. 구현이 실제로 막아낸 함정

설계 §12가 예고한 함정 중 **구현·테스트로 실증된 것**만 적는다.

| 함정 | 어떻게 막았는지 (증거) |
|------|----------------------|
| `/state` 본문이 현재시각에 의존하면 ETag가 거짓말을 한다 | `FixedClock`을 9시간 진행(10:00→19:00 HKT)시켜도 `json()`·`content`·`ETag`가 **전부 동일**함을 테스트로 고정 |
| 재정렬 시 `UNIQUE(day_id, position)` 중간 상태 충돌 | 4스팟 일자의 **24개 순열 전부** + 6스팟 회전 6회 테스트. 2개짜리로만 봤으면 통과했을 자리 |
| `version`(리소스 잠금)과 `revision`(여행 동기화) 혼동 | 서로의 자리에 넣으면 각각 409, 서로의 필드명으로 보내면 422 — 양방향 테스트 |
| 캐시 만료 순간 동시 요청이 전부 외부로 나감 | 스레드 2개가 동시에 만료를 만나도 외부 호출 **1회**(single-flight). `.calls` 카운터로 측정 |
| 파이썬 `round()`의 은행가 반올림이 JS와 갈림 | `util.round_half_up` 강제. 진행률 퍼센트는 **서버 값을 프론트가 그대로 그린다**(재계산하지 않는다) |
| 영업시간 파서의 가짜 경고 | 시드 27건 중 **6건을 `unknown`으로 남기고 경고를 만들지 않는다**(§6 참조) |
| 벤더 파일 CRLF 변환으로 해시 붕괴 | `.gitattributes` + `tools/vendor_check.py`. 1바이트 변조·파일 삭제·낯선 파일 추가를 **실제로 탐지시켜 확인** |

---

## 6. 영업시간 파서 — 시드 27건 분류 실측

| 분류 | 건수 |
|------|------|
| `open_24h` (상시) | 4 |
| `open_range` (시간 범위 확정) | 17 |
| `unknown` (해석 불가 → **경고 생성 안 함**) | 6 |

`unknown`으로 남긴 6건은 전부 **한 문자열에 두 대상의 시간이 섞였거나 모호한 표현**이다:
`매일 20:00 시작 · 약 10~13분` / `단지 상시 · 상점 대략 11:00–20:00` /
`하행 06:00–10:00 / 상행 10:00–24:00` / `저녁~심야` /
`야외 상시 / 내부 대략 10:00–17:30` / `매일 09:00–18:00 / 채식당 11:30–16:30`.

이것이 AC-024가 요구한 동작이다 — 무리하게 해석해 가짜 경고를 만들면 **진짜 경고가 묻힌다.**

---

## 7. 검증 실측

| 항목 | 결과 |
|------|------|
| 전체 테스트 | **521 passed** (domain 136 · api 163 · static 40 · 기반 182) |
| 린트 | `ruff check .` All checks passed |
| 의존성 고정 | `check_pinning.py` 위반 0건 (락파일 + 액션 고정 13건) |
| 서버 기동 | `python -m harbor_lantern` → `/api/health` 200 · `/` 200 |
| `GET /state` 처리시간 | p50 **4.64 ms** · p95 5.24 ms · p99 6.79 ms (27스팟 전량 재계산, 200표본) |
| `POST /optimize` 처리시간 | p50 **2.60 ms** · p95 3.04 ms (자유 9스팟) |
| 프론트↔API 필드명 대조 | 렌더 조각 59개·36,137자에 `undefined`/`NaN` **0건**. 쓰기 payload 6종도 실서버 대조 |
| 클라이언트↔서버 계산 일치 | 하버사인 442m 동일 · 길찾기 URL 문자열 완전 일치 |

성능 수치의 환경 의존성은 `reports/perf.md`가 스스로 적는다(Python 3.12.13 · Windows 11 AMD64).
CI는 목표치로 실패하지 않고 **파탄 상한(p95 > 1000ms)** 에서만 실패한다 — 간헐적으로 빨개지는 게이트는
아무도 보지 않게 되기 때문이다.

---

## 8. 미해결 / 후속

| # | 항목 | 처리 |
|---|------|------|
| 1 | 브라우저 실제 렌더(픽셀) 미확인 | 헤드리스 브라우저가 없어 **DOM 스텁 + 실데이터 주입**까지가 이 환경의 한계. 실기기 확인 필요 — 릴리스 노트에 명시 |
| 2 | `leaflet.js.map` 미벤더링 | 설계 §9 파일 목록 준수. 개발자도구를 열 때만 소스맵 404. 런타임·외부 요청에 영향 없음 |
| 3 | `tools/export_pages.py` 경로 | 요구사항 범위 밖 추가분. MVP 검증 대상이 아니며 기본값에서 동작 동일 |
| 4 | `docs/04`·`docs/00` 미작성 | Phase 4·6의 몫. 현재 `gate.py`의 문서 계약 단계가 이 때문에 실패한다 |

---

## 9. 추적성

- 선행: `01_요구사항정의서.md`(REQ-001~016 · NFR-001~016 · AC-001~049) → `02_설계서.md`(DSN-01~25)
- 본 문서: DSN-01~25 → 구현 파일 (§3.1), REQ-001~016 → 구현 진입점 (§3.2)
- 후행: `04_테스트결과서.md`(TC ↔ AC) · `00_산출물_인덱스.md`(전체 추적성 매트릭스)


## 10. 지역 여행 확장 구현 (2026-09-14)

REQ-019 / DSN-28: 새 메인, 여행지 검색·선택, 날짜와 여행 속도/관심사/반경 입력,
브라우저 최근 여행 보관·JSON 내려받기·홍콩 가이드 이동을 구현했다.

REQ-020 / DSN-29: 날짜별 동선·식사 시간 선호·영업시간 내 체류·중복 방문 제외를 구현했다.
후보 부족·복잡한 시간 규칙은 숨기지 않고 자유시간/미확인으로 표시한다.

REQ-021 / DSN-30: 현재 위치 권한 요청·지역 대체 검색·반경/거리/평점 정렬,
OSM 영업시간·음식 종류·메뉴 원문과 선택적 Google 평점/후기/작성자를 연결했다.
실제 메뉴와 평점 데이터가 없는 상황에서는 추정값을 생성하지 않는다.
