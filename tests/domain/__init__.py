"""IMP-A 도메인 계산 엔진의 단위 테스트 (T1).

여기 있는 테스트는 **네트워크·DB·현재 시각을 쓰지 않는다.** 도메인이 순수하기
때문이며, 그래서 전부 밀리초 단위로 끝난다(설계서 §2.1 · NFR-003).

각 테스트 이름·docstring 에 검증하는 AC 를 적는다 — Phase 4 가 그것을 근거로
`docs/04_테스트결과서.md` 의 TC↔AC 매트릭스를 쓴다.
"""
